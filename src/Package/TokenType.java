package Package;

/**
 * Created by Lenovo on 3/10/2015.
 */
public enum TokenType {IF, THEN, ELSE, END, REPEAT, UNTIL, READ, WRITE, ID, NUM, PLUS, MINUS, TIMES, DIVISION, ASSIGN, EQ, LT, GT, SEMI, LPAREN, RPAREN, ERROR, EOF}
